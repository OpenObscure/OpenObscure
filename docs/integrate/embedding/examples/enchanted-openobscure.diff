diff --git a/Enchanted/Stores/ConversationStore.swift b/Enchanted/Stores/ConversationStore.swift
index c9043ea..4690dd5 100644
--- a/Enchanted/Stores/ConversationStore.swift
+++ b/Enchanted/Stores/ConversationStore.swift
@@ -10,6 +10,7 @@ import SwiftData
 import OllamaKit
 import Combine
 import SwiftUI
+import OpenObscureLib

 @Observable
 final class ConversationStore: Sendable {
@@ -134,19 +135,41 @@ final class ConversationStore: Sendable {
         let userMessage = MessageSD(content: userPrompt, role: "user", image: image?.render()?.compressImageData())
         userMessage.conversation = conversation

-        /// prepare message history for Ollama
+        /// prepare message history for Ollama — sanitize user messages via OpenObscure
+        let oo = OpenObscureManager.shared
         var messageHistory = conversation.messages
             .sorted{$0.createdAt < $1.createdAt}
-            .map{OKChatRequestData.Message(role: OKChatRequestData.Message.Role(rawValue: $0.role) ?? .assistant, content: $0.content)}
+            .map { msg -> OKChatRequestData.Message in
+                let role = OKChatRequestData.Message.Role(rawValue: msg.role) ?? .assistant
+                if role == .user {
+                    let (sanitized, _) = oo.sanitize(msg.content)
+                    return OKChatRequestData.Message(role: role, content: sanitized)
+                }
+                return OKChatRequestData.Message(role: role, content: msg.content)
+            }


         print(messageHistory.map({$0.content}))

-        /// attach selected image to the last Message — sanitize via OpenObscure first
+        /// attach selected image to the last Message — sanitize via OpenObscure
         if let image = image?.render() {
             if let lastMessage = messageHistory.popLast() {
-                let imagesBase64: [String] = [image.convertImageToBase64String()]
-                let messageWithImage = OKChatRequestData.Message(role: lastMessage.role, content: lastMessage.content, images: imagesBase64)
+                var base64String: String
+                if let imageData = image.compressImageData() {
+                    do {
+                        let sanitized = try sanitizeImage(
+                            handle: oo.handle,
+                            imageBytes: imageData
+                        )
+                        base64String = sanitized.base64EncodedString()
+                    } catch {
+                        // Fail-open: use original image if sanitization fails
+                        base64String = image.convertImageToBase64String()
+                    }
+                } else {
+                    base64String = image.convertImageToBase64String()
+                }
+                let messageWithImage = OKChatRequestData.Message(role: lastMessage.role, content: lastMessage.content, images: [base64String])
                 messageHistory.append(messageWithImage)
             }
         }
@@ -192,7 +215,14 @@ final class ConversationStore: Sendable {
         if messages.isEmpty { return }

         if let responseContent = response.message?.content {
-            currentMessageBuffer = currentMessageBuffer + responseContent
+            let restored = OpenObscureManager.shared.restore(responseContent)
+
+            // Cognitive firewall: scan restored response for manipulation
+            if let riReport = OpenObscureManager.shared.scanResponse(restored) {
+                print("[OpenObscure] RI: severity=\(riReport.severity) cats=\(riReport.categories)")
+            }
+
+            currentMessageBuffer = currentMessageBuffer + restored

             throttler.throttle { [weak self] in
                 guard let self = self else { return }
diff --git a/Enchanted/UI/iOS/ChatView_iOS.swift b/Enchanted/UI/iOS/ChatView_iOS.swift
index 95e8b07..1c7d6db 100644
--- a/Enchanted/UI/iOS/ChatView_iOS.swift
+++ b/Enchanted/UI/iOS/ChatView_iOS.swift
@@ -147,7 +147,7 @@ struct ChatView: View {
                     .font(.system(size: 14))

                 RecordingView(speechRecognizer: speechRecognizer, isRecording: $isRecording.animation()) { transcription in
-                    self.message = transcription
+                    self.message = OpenObscureManager.shared.sanitizeTranscript(transcription)
                 }
             }
             .onChange(of: isFocusedInput, { oldValue, newValue in
diff --git a/Enchanted/UI/macOS/Chat/Components/InputFields_macOS.swift b/Enchanted/UI/macOS/Chat/Components/InputFields_macOS.swift
index bca42bf..1232661 100644
--- a/Enchanted/UI/macOS/Chat/Components/InputFields_macOS.swift
+++ b/Enchanted/UI/macOS/Chat/Components/InputFields_macOS.swift
@@ -95,7 +95,7 @@ struct InputFieldsView: View {
                 HStack {
                     RecordingView(isRecording: $isRecording.animation()) { transcription in
                         withAnimation(.easeIn(duration: 0.3)) {
-                            self.message = transcription
+                            self.message = OpenObscureManager.shared.sanitizeTranscript(transcription)
                         }
                     }
